import { defineMessages } from 'react-intl';

export const scope = 'containers.contactUs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Contact Us',
  },
});
