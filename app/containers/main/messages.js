import { defineMessages } from 'react-intl';

export const scope = 'containers.main';

export default defineMessages({
  pageDescription: {
    id: `${scope}.pageDescription`,
    defaultMessage:
      'Moon Palace Referrals, your next luxury vacation destination',
  },
  defaultTitle: {
    id: `${scope}.defaultTitle`,
    defaultMessage: 'Moon Palace Referral',
  },
});
